# FAVICON (avoid console error)
favicon:
  path: /favicon.ico
  controller:
  methods: GET

products:
  resource: routes/products.yaml
  prefix: /api/

carts:
  resource: routes/carts.yaml
  prefix: /api/
